

UPDATES:

GRANT
- I've made some changes to how BICI saves related to the whole single file vs data directory. So previously after a simulation, inf or post-sim was performed it wasn't possible to interchange between these. Now these restrictions no longer apply.
- Generate errors if different transition times occur at the same time point (which is logically impossible according to the model).


LAURA


JAMIE

CHRIS
- I've put in error checking to make sure that individual effect names do not contain spaces 
- Corrected buy whereby simulation model was always being used
- Various bugs in the interface have been corrected related to testing lots of sim, inf, post-sim and ext options.
- Corrected bug where simulation results disappearing after loading inference results
- Made some changes in how data tables are loaded (got rid of no heading option and stopped simulated data being added).
- Corrected some errors with data checking when the model is removed
- Created an error if compartmental observations clash with transition observations
- Generate error if inconsistent compartmental observations